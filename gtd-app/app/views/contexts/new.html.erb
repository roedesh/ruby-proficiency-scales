<% content_for :title, t('contexts.new.new_context') %>

<h1><%= t('contexts.new.new_context') %></h1>

<%= render "form", context: @context %>

<br>

<div>
  <%= link_to t('contexts.new.back_to_contexts'), contexts_path %>
</div>
